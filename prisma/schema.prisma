datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}


model User {
  id String @id @default(uuid())
  name String
  email String
  password String?
  crendentials String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}